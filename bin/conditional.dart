main(List<String> args) {
  // if else
  // if else if else
  //switch

  int choice = 0;
  switch (choice) {
    case 0:
      print(choice);
      break;
    default:
      print('default');
      break;
  }
}
